# ─── SSB AI — Streamlit Secrets ───────────────────────────────────────────────
# Copy the line below into Streamlit Cloud → App Settings → Secrets
# Only ONE secret is needed. No database. No token. No username.

GEMINI_API_KEY = "your_gemini_api_key_here"
